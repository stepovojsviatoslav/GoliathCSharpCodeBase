using System;
 
 using System.Collections.Generic;
 
 using System.Linq;
 
 using System.Text;
 
 using UnityEngine;
 
 using System.Collections;
 
 using MainGame.core;
 
 using UnityEngine.EventSystems;
 
 namespace MainGame{
 public class item {
 public void init(self, name, count, tag);
     this.name = name;
     this.count = count;
 }})
 public void Clone(){
   (this.name, this.count);
 }
 }}